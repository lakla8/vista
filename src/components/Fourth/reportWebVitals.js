// reportWebVitals.js - аналитика производительности (упрощенная версия)

const reportWebVitals = onPerfEntry => {
  if (onPerfEntry && onPerfEntry instanceof Function) {
    // Упрощенная версия без web-vitals пакета
    console.log('Performance monitoring is available but web-vitals package not installed');
  }
};

export default reportWebVitals; 